{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "db:generate", "db:push"]
    },
    "clean": {},
    "db:generate": {
      "cache": false
    },
    "db:push": {
      "cache": false
    }
  },
  "experimentalUI": true
}
